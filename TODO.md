# 최우선 작업 (TOP PRIORITY)

> 사용자가 직접 자료를 주기로 한 항목. 자료 받으면 즉시 반영 후 이 목록에서 지운다.

## 1. 사진·로고 — 전부 완료

- ~~CEO 7번 정용진 로고~~ → 사용자 결정: **로고 없이 기업명 텍스트만** 표시 (2026-09-07)
- ~~CEO 10번 김범수 사진~~ → 사용자 제공 사진 `images/cp_kimbs.jpg` (695×453, 2026-09-07)
- ~~CEO 23번 정몽구 사진~~ → 사용자 제공 사진 `images/cp_mongkoo.jpg` (512×344, 2026-09-07)
  명찰 노출 없는 정면 단독 컷. 모자이크 5단계 재현 확인.

## 2. 완료된 검증

- ~~국내 로고 퀴즈 '신세계' 로고 검증~~ → **오류 확인 후 교체 완료** (2026-09-07)
  기존 `web_ffe4e2472fee.webp`는 한자 `新世界`(중국 New World 계열)였음.
  실제 신세계 로고(붉은 꽃 + SHINSEGAE)로 교체하고 어두운 글자라 `whiteBg: true` 지정.

## 3. 구현 완료 — 랜덤 출제(순서 코드)

2026-09-07 구현. 롤백 기준점: 태그 `v1.3-pre-random` (커밋 7672185).

- 앱을 열면 네 자리 **순서 코드**가 생성되어 홈 화면에 표시됨. 코드+게임이름을 씨앗으로 순서를 정한다.
- 같은 코드 = 항상 같은 순서(재현 가능). 코드 하나로 전체 게임의 순서가 정해진다.
- 빔(관객) 창: 주소(`?seed=`)로 코드를 받고, 발표자 방송으로도 계속 동기화된다(발표자가 기준).
- 관리자 정답지: 상단에 코드 입력칸. 다른 기기에서 코드를 넣으면 정답지 번호가 출제 순서와 일치한다.
- 홈 '새 순서' 버튼: 코드를 새로 뽑아 전체를 다시 섞고 빔 창에도 알린다.
- 고정 자리: 전체 1~2번 / CEO 1·2·6번 / HS출연자 1·2·3·6번 / 등장인물 1·2·5~9번 / 기업초성 1~3번
- 같은 세션에서 같은 게임을 다시 켜면 순서는 같다(코드가 같으므로). 바꾸려면 '새 순서'.

## 4. 진행 중 — 음악 퀴즈 시작 지점 검수 (대표님 작업 대기)

문제: 미리듣기 30초가 전주·1절로 시작해 후렴이 안 나오는 곡이 있었음. 118곡 분석 결과 39곡이
5초 이상 기다려야 노래가 시작됨(윤수일 '아파트'는 앞 5초가 거의 무음).

완료된 것
- 음원 118곡 로컬 저장(`audio/`, 128kbps AAC, 54.5MB) — 외부 음원 참조 0건
- 곡 데이터에 `start`(초) 지원. 자동 분석으로 91곡에 기본값 지정(전주 건너뛰기 기준)
- 진행자 실시간 구간 이동: `[` 5초 뒤로 / `]` 5초 앞으로 / `\` 시작점 복귀 + 화면 버튼(빔에는 숨김)

남은 것 — **검수 페이지에서 118곡 시작 지점 확정**
- https://primong12.github.io/quiz/tools/start-review.html
- 파형 클릭으로 지점 지정, ✓로 확인 표시, 진행 상황은 브라우저에 자동 저장
- 다 끝나면 '클립보드 복사' 눌러 결과를 전달 → index.html에 반영

~~미사용 `SONG` 배열~~ → 삭제 완료 (2026-09-08). 이제 외부 음원 참조 0건, 미사용 파일 0개.

## 5. 완료 — 애니메이션 퀴즈 (25/25)

이미지 25작품 전부 확보(Kitsu 배너 20장 + 사용자 제공 5장), 게임 hidden 해제, 홈 노출 확인.
사용자 제공: 리리카 SOS · 신세기 사이버 포뮬러 · 파이팅 대운동회 · 체포하겠어 · 웨딩피치

## 6. 완료 — 행사 안정성 보강 (2026-09-08)

- **완전 오프라인 동작**: 아이콘을 외부 CDN(unpkg)에서 받던 것을 로컬화(fonts/phosphor.css + Phosphor.woff2,
  쓰는 아이콘 18개 규칙만 추려 76KB → 1.9KB). index.html의 외부 리소스 참조 0건.
- **팀 점수 세션 저장**: 행사 중 새로고침·브라우저 사고에도 점수 유지(sessionStorage).
- **곡별 음량 평준화**: 재생 구간 실측 결과 편차 18.1dB(-24.3 ~ -6.2dB)이었음.
  목표 -20dB 기준으로 111곡에 `vol` 지정(재인코딩 없이 재생 볼륨만 조절 — 음질 손실 0).
  이미 조용한 7곡은 1.0 유지. → 전체 볼륨이 낮아졌으니 스피커 볼륨을 한 번 올려두면 됨.
- **팝업 차단 안내**: 빔 창이 브라우저에 막히면 해결 방법을 알려주는 안내창 추가.
- **검수 도구 v2**: 후렴 후보 초록 마커(눌러서 즉시 적용) · 실제 행사와 같은 볼륨으로 재생 ·
  15초 재생 · '다음 미검수 곡' 버튼과 N 단축키.
- 애니메이션 저해상도 이미지 5장은 미리 확대해도 개선이 없어(원본 해상도 한계) 그대로 유지.
  더 큰 파일이 생기면 교체: 리리카 SOS(400x308) · NG기사 라무네(600x337) ·
  파이팅 대운동회(469x353) · 체포하겠어(248x363) · 웨딩피치(600x450)

## 참고 — 의도적으로 남겨둔 것 (버그 아님)

- CEO 퀴즈에 정답이 같은 문항이 3쌍 있음: 애플(팀 쿡·스티브 잡스), 마이크로소프트(나델라·빌 게이츠),
  현대자동차(정의선·정몽구). 사용자가 "둘 다 그대로 두기"로 결정함.
